# 公开发行边界

本发行版从维护工作区中按文件选取通用代码与协议，在目标仓库既有初始提交上构建；不引入维护工作区的 Git 历史。

## 公开内容

- 六阶段 Skills、通用治理/审批/恢复协议、schema、验证脚本。
- 从源码生成的空白 Word 结构（只发布生成器，不发布现有 DOCX）。
- 合成测试、结果合同示例、安装说明与入口；示例数值不来自真实比赛。
- 私有知识库入口替换为通用空白配置；原用户偏好替换为公开默认值；组件历史观测置空。

## 排除内容

原始题面与附件、论文、资料全文、OCR、截图、训练和评测题库、案例知识卡、来源登记与笔记、运行结果、审计历史、发布快照、本地配置与缓存、个人机器路径、密钥，以及现有二进制模板和图像均不进入公开清单。

## 可重复检查

`PUBLIC_MANIFEST.json` 逐文件绑定 SHA-256；`.gitignore` 默认忽略全部文件，仅放行清单中的精确路径。`python tools/audit_public.py` 检查白名单内容、禁止路径、二进制/超长文件、敏感特征及 Git index 与工作副本的一致性。CI 在安装和生成本地文件前执行审计。

修改内容必须先复核，再更新清单中的相应哈希。新增文件须同时更新精确白名单与清单；不要使用目录通配放行。本地运行产物始终在公开清单之外。模式扫描不是资料语义审查的替代，未来发布仍须逐项确认新增内容的来源与授权。

此次验证包括入口引用闭包、全部回归测试及合成任务的数值计算、候选晋级、恢复、批准复用和缺失成本记录。真实竞赛模型质量、宿主 Agent 权限与 Word/PDF 视觉交付需要在使用者环境另行验证。
