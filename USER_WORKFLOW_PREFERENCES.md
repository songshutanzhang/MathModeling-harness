# 可配置工作流默认值
这是公开版通用默认值，不包含原维护者的个人偏好。使用者的明确要求优先。

- 在独立项目目录保存题目、数据、论文、审批、日志与密钥，勿写入 Harness 仓库。
- training_run 默认 Tier 1；评测、正式赛和发布候选按治理策略执行完整验收。
- Tier 2/3 默认以 Word 为正文编辑源，PDF 为排版快照；逐轮保留版本并检查页面。
- 读取当前实际模型能力，不假定任何厂商模型、推理档位或独立代理接口可用；缺失能力如实记录。
- 只有用户明确批准当前决策包才记录审批；既有有效批准在恢复时复用。
- 历史案例和知识卡由使用者自行提供，本发行版不包含这些资料。
